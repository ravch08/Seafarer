export type SectionHeadingProps = {
  heading: string;
  superHeading: string;
  bgText: string;
  description: string;
};

export type PageBannerProps = {
  page: string;
  imgSrc: string;
  description: string;
};

export type TourBookingProps = {
  bookingClass: string;
};
